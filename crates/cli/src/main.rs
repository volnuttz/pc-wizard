//! Future native CLI entry point.
//!
//! Python remains the shipped behavioral reference until this binary passes the
//! shared compatibility suite.

fn main() {
    eprintln!("pc-wizard Rust migration workspace: implementation has not started");
    std::process::exit(2);
}
